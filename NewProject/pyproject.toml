[project]
name = "insurance-rag"
version = "0.1.0"
description = "Multi-agent RAG PoC for insurance claim ingestion and adjudication"
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
